{
    "fields": [{
            "name": "attribute_id",
            "type": "number"
        },
	{
            "name": "value",
            "type": "string"
        }
    ],

    "validations": [{
            "field": "name",
            "rules": "required"
        }
    ]
}
